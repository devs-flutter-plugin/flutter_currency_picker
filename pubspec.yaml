name: currency_picker
description: A Flutter package to select a currency from a list of currencies.

homepage: https://github.com/devs-flutter-plugin/flutter_currency_picker
repository: https://github.com/devs-flutter-plugin/flutter_currency_picker

version: 2.1.0

environment:
  sdk: '>=3.13.0 <4.0.0'
  flutter: '>=3.47.0'

dependencies:
  collection: ^1.19.1
  flutter:
    sdk: flutter
  material_ui: ^1.2.0

flutter:
  assets:
    - lib/src/res/

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.8.0
